server:
  port: 8760

eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
  server:
    wait-time-in-ms-when-sync-empty: 5

eureka:
  server:
    enable-self-preservation:
      false

